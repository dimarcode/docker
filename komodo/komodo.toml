[[server]]
name = "Local"
[server.config]
enabled = true

##

[[server]]
name = "test-lxc"
tags = ["testing"]
[server.config]
address = "https://test-lxc:8199"
enabled = true

##

[[stack]]
name = "test-mealie"
[stack.config]
server = "test-lxc"
poll_for_updates = true
git_account = "dimarcode"
repo = "dimarcode/docker"
run_directory = "docker-compose/mealie"
file_paths = ["docker-compose.yaml"]

##

[[repo]]
name = "docker-test-repo"
[repo.config]


##

[[procedure]]
name = "Backup Core Database"
description = "Triggers the Core database backup at the scheduled time."
tags = ["system"]
config.schedule = "Every day at 01:00"

[[procedure.config.stage]]
name = "Stage 1"
enabled = true
executions = [
  { execution.type = "BackupCoreDatabase", execution.params = {}, enabled = true }
]

##

[[procedure]]
name = "Global Auto Update"
description = "Pulls and auto updates Stacks and Deployments using 'poll_for_updates' or 'auto_update'."
tags = ["system"]
config.schedule = "Every day at 03:00"

[[procedure.config.stage]]
name = "Stage 1"
enabled = true
executions = [
  { execution.type = "GlobalAutoUpdate", execution.params = {}, enabled = true }
]

##

[[builder]]
name = "Local"
[builder.config]
type = "Server"
params.server_id = "Local"

##

[[resource_sync]]
name = "github-test-sync"
[resource_sync.config]
repo = "dimarcode/docker"
git_account = "dimarcode"
resource_path = ["komodo/komodo.toml"]
managed = true
delete = true